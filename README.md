# Motion Detection Project

Detects motion in a video and presents it.

## Setup and Run

Install python requirements by running:  
```python3 -m pip install -r requirements.txt```

Setup VIDEO_PATH environment variable to contain the video path you want to run the system with.

Deploy the systems with your video by running:  
```python3 deploy_systems.py```

